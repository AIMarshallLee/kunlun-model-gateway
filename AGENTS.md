# Kunlun Gateway 开发规则

本目录是独立的公共模型网关控制面，不得进入公众号客户桌面包。

## 安全不变量

- `PUBLIC_SIGNUP`、测试支付和真实上游默认关闭；不得把测试回调描述为真实收款。
- 金额全部使用整数微美元（`microUSD`）；余额只能通过只追加双式账本改变。
- 支付回调先验证原始请求体签名，再校验订单、金额、状态和幂等键。
- 密码、会话令牌、客户 API Key、支付密钥和上游密钥不得明文入库或写入日志。
- 不保存提示词、回答、请求正文或正文哈希；审计只保存请求 ID、模型、Token、成本和失败类别。
- 只有明确的连接失败、429 和 5xx 可以故障切换；参数、认证、审核拒绝和不确定读取超时不得切换。
- Provider 调用不得放在数据库事务中；必须先提交预授权，再调用，再独立结算。
- 生产环境必须使用 PostgreSQL、外部限流/熔断、TLS/WAF、KMS/Secret Manager 和正式支付 SDK。

## 验证要求

- 先写失败测试，再实现。
- 修改后运行本目录全部测试和覆盖率，并回归父项目测试。
- 任何“公网可用”“真实充值”“生产就绪”结论都必须有对应实网证据。
