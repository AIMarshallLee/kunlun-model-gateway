# 安全策略

本仓库当前是候选生产基线，默认关闭公共注册、测试支付和真实上游。部署者仍须自行配置 PostgreSQL、TLS/WAF、外部限流、KMS/Secret Manager 和正式支付 SDK；通过测试、合并或本地运行不等于公网或生产安全证明。

## 报告安全问题

请不要在公开 Issue、Pull Request、日志或聊天中披露可利用的漏洞、密钥、客户数据或复现所需的敏感数据。优先使用 GitHub 的 Security Advisories 私下报告功能（如果仓库启用）；如果该功能不可用，请先联系仓库维护者并提供最少必要的复现信息，等待确认后再公开细节。

报告中请包含：受影响版本或提交、影响范围、最小复现步骤、潜在影响、建议修复方向，以及是否已经在真实环境中观察到。请移除真实令牌、支付数据、个人信息、提示词正文和客户素材。

我们会先确认问题、评估严重度和影响范围，再决定修复、缓解和公开时间。不要因为没有收到即时回复而继续公开敏感细节。

## 部署安全边界

- 每个客户使用独立密钥、预算和用量台账；密钥只通过 Secret Manager 或等价安全机制注入。
- 不提交 `.env`、数据库文件、备份、Provider Key、支付密钥或生产日志。
- 不把默认关闭的公共注册、充值或真实 Provider 开关直接打开，也不要将测试回调宣传为真实支付。
- 生产环境必须先完成迁移、预检、备份恢复演练、限流/熔断和日志脱敏验证。
