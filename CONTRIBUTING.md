# 贡献指南

感谢你对 Kunlun Model Gateway 的关注。本仓库是一个模型网关控制面候选生产基线，面向需要预算、成本台账、人工接管和多供应商故障切换的垂直业务。它不是共享账号、低价 Key、匿名充值或无限量的公共中转服务。

## 开始之前

- 阅读 [README.md](README.md)、[AGENTS.md](AGENTS.md) 和 `docs/` 下的运行说明。
- 使用 Python 3.12，并优先使用仓库中的 `uv.lock` 安装依赖：`uv sync --locked --extra postgres --extra test`。
- 不要提交密钥、客户素材、提示词正文、回答正文、支付凭证或本地数据库文件。
- 默认保持 `PUBLIC_SIGNUP`、测试支付和真实上游关闭。测试回调不能被描述为真实收款。

## 提交变更

1. 为可复现的缺陷或需求先开 Issue；小范围修复也可以直接提交 Pull Request。
2. 每个 Pull Request 聚焦一个问题，说明背景、变更、风险、验证命令和是否涉及迁移。
3. 新行为先补失败测试，再实现；涉及账本、预算、密钥、支付、供应商调用或租户隔离时，必须补边界测试。
4. 所有变更都应通过 Python 3.12 的全量测试与覆盖率检查。CI 只做验证，不部署任何环境。
5. 数据库迁移应保持可回滚思路，并说明对已有数据的影响。Provider 调用不得放在数据库事务中。

## Pull Request 评审重点

- 金额是否始终使用整数微美元，余额是否只能通过追加式双式账本改变；
- 密码、会话令牌、客户 API Key、支付密钥和上游密钥是否不会入库或进日志；
- 故障切换是否仅针对明确的连接失败、429 和 5xx；参数、认证、审核拒绝和不确定读取超时不得静默切换；
- 是否保留人工审核与草稿箱边界，不引入自动群发或自动原创声明；
- 是否更新文档、迁移说明和测试，并且没有把本地测试写成公网、真实支付或生产证据。

提交代码即表示你同意其按 [Apache-2.0](LICENSE) 提供，并确认你有权提交该代码。
